// Library imports
import { Input } from 'atoms';
import React, { ReactElement } from 'react';

export const Root = (): ReactElement => {
	return (
		<div>
			<h1>Root Page</h1>
			<Input />
		</div>
	);
};
